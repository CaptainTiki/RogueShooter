extends Control
class_name WB_WeaponStats

func show_weapon(weapon: Weapon) -> void:
	#if weapon == null or weapon.stats == null:
		#return
	#%DamageLabel.text = str(weapon.stats.damage)
	#%AmmoLabel.text = str(weapon.stats.ammo_capacity)
	pass
